# 表达式类别：左值，右值

# RVO / NRVO

返回值直接构造在调用栈，而非当前函数栈

RVO是匿名对象，在C++17开始强制RVO。

NRVO是具名对象，已在当前函数栈内尝试构造过的对象，会直接构造到调用栈上。
标准不强制，但几乎所有都实现
``` C++
struct T {
    T() = default;
    T(const T&) = delete;
    T(T&&) = delete;
};

T f() {
    T x;
    return x;   // 只要 NRVO 生效，这就可以通过编译。debug或者-O0会关闭NRVO导致失败
}
```

# 完美转发

std::forward 依靠引用坍缩规则